- name: Display Test Metadata
        run: |
          echo "### 🔍 External Test Metadata" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---:|:---|" >> $GITHUB_STEP_SUMMARY
          # Fixed the 'SET' to 'STEP' below
          echo "| **Branch** | \`${{ github.event.client_payload.branch }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ github.event.client_payload.commit }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Source** | ${{ github.event.client_payload.source }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Status** | ${{ github.event.client_payload.success == 'true' && '✅ Success' || '❌ Failure' }} |" >> $GITHUB_STEP_SUMMARY
